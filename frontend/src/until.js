import toast from "react-toastify"
export const handleSuccess=(mgs) =>{
    toast.success(mgs,{
        positon:'top-right'
    })
}

export const handleError=(mgs) =>{
    toast.success(msg,{
        positon:'top-right'
    })
}